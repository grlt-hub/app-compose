---
title: literal
description: App-Compose — literal reference
---

Wraps a value as data — not a reference to a Task or Tag.

`literal<const T>(value)` — Returns a `Spot<T>`. Use for values you already know upfront — a primitive or object
