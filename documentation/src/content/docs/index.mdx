---
title: App-Compose
description: Get started building your docs site with Starlight.
template: splash
hero:
  tagline: The library for dependency injection
  actions:
    - text: Learn
      link: /start/
      icon: open-book

    - text: API Reference
      link: https://starlight.astro.build
      variant: secondary
head:
  - tag: style
---

import Sandbox from "../../components/sandbox/index.astro"
import example from "./example.js?raw"
import logger from "./logger.js?raw"
import featureToggle from "./feature-toggle.js?raw"

<Sandbox template="vanilla" code={example} options={{ editorHeight: 440 }} />

## Reuse code for any task

Write logic once and run it anywhere. Need to mock an API or change a config for a specific page? Just provide different values<br/>
Build features that work in any part of the app.

<Sandbox template="react-ts" code={logger} options={{ editorHeight: 440, showConsole: true }} />

## Toggle features with ease

Enable or disable any feature with a single toggle. Turning off a feature automatically stops all its dependent features.<br/>
Stop cluttering code with `if` conditions and manual cleanup.

<Sandbox template="vanilla" code={featureToggle} options={{ editorHeight: 470 }} />
