---
title: Using TypeScript
description: Type your App-Compose code so TypeScript catches mismatches on supply and read.
---

import Sandbox from "../../../../components/sandbox/index.astro"
import tagType from "./tag-type.ts?raw"

App-Compose is written in TypeScript. This page shows how to type your code.

For the full list of exported types, see [reference/types](/reference/types)

## Typing a Tag

A Tag holds a typed value. TypeScript then catches mismatches on both ends — supply and read.

<Sandbox anchor="tag-type"
  template="vanilla-ts"
  code={tagType}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>
