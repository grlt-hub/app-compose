---
title: Using TypeScript
description: App-Compose — Using TypeScript
---

import Sandbox from "../../../../components/sandbox/index.astro"
import tagType from "./tag-type.ts?raw"
import taskResult from "./task-result.ts?raw"
import taskType from "./task-type.ts?raw"
import loggerType from "./logger-type.ts?raw"
import stageConfigType from "./stageConfig-type.ts?raw"

App-Compose comes with built-in TypeScript support, so you can start writing type-safe code right away.

## TypeScript with Tags

When creating a Tag, you can specify the data type it holds.

<Sandbox
  template="vanilla-ts"
  code={tagType}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>

This provides full type safety for both Tag inputs and outputs.

## Useful Types

### TaskResult&lt;T&gt;

Extracts the return type of a given Task.

<Sandbox
  template="vanilla-ts"
  code={taskResult}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>

### TaskStatus

Represents the possible execution outcomes of a Task.

{/* oxfmt-ignore */}

```ts
type TaskStatus = "done" | "fail" | "skip"
```

### Task&lt;T&gt;

Represents a Task created by `createTask`. Use it to type function parameters or variables that hold a Task reference.

<Sandbox
  template="vanilla-ts"
  code={taskType}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>

### ComposeLogger

Defines lifecycle hooks for observing stage and task execution.

<Sandbox
  template="vanilla-ts"
  code={loggerType}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>

### StageConfig

Represents the configuration for a single stage passed to `compose`.

<Sandbox
  template="vanilla-ts"
  code={stageConfigType}
  options={{ layout: "console", showConsoleButton: false, hideOutput: true }}
/>
