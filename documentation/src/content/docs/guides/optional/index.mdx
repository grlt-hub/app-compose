---
title: Optional values
description: Mark a value as not required — wherever it flows between Tasks and Wires. The Task still runs when the source is missing.
---

import Sandbox from "../../../../components/sandbox/index.astro"
import context from "./context.ts?raw"
import wire from "./wire.ts?raw"
import shape from "./shape.ts?raw"

Values flow between Tasks and Wires. Every value a Task reads is required by default. `optional` marks one as not required — when the source is missing, the Task still runs and the field is `undefined`.

## Inside a context

`optional` works in both `run.context` and `enabled.context`.

<Sandbox
  template="vanilla-ts"
  code={context}
  options={{ layout: "console", showConsoleButton: false, editorWidthPercentage: 70 }}
/>

## Inside a Wire

`optional` works in `createWire.from`. The destination tag carries `T | undefined`.

<Sandbox
  template="vanilla-ts"
  code={wire}
  options={{ layout: "console", showConsoleButton: false, editorWidthPercentage: 70 }}
/>

## Inside a shape

`optional` works in `shape`. The callback receives `T | undefined`.

<Sandbox
  template="vanilla-ts"
  code={shape}
  options={{ layout: "console", showConsoleButton: false, editorWidthPercentage: 70 }}
/>
