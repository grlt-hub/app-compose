---
import SandpackEditor from './index.tsx';

const { code, template, options, files } = Astro.props;
---

<SandpackEditor client:load code={code} template={template} options={options} files={files} />
