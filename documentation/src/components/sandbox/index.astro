---
import SandpackEditor from './index.tsx';

const { code, template, options, files } = Astro.props;
---

<code class='llms-only'>{code}</code>

<SandpackEditor client:only="react" code={code} template={template} options={options} files={files} />
