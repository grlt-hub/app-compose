name: Publish

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    if: github.repository == 'grlt-hub/app-compose'
    runs-on: ubuntu-latest
    container:
      image: node:24.8.0-slim
    environment: Release

    steps:
      - name: Publish (stubbed)
        run: echo "would publish ${GITHUB_REF_NAME}"
